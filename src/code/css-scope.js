const test = `<Card class="card--big"></Card>

// Card Component
<div class="card">
  <div class="headline u-text--white">
    {{ headline }}
  </div>
  <div class="description u-margin--small">
    {{ description }}
  <div>
</div>

.card { background-color: red; }
.headline { font-size: 24px; }
.description { font-size: 14px; }
.u-text--white { color: white; }
.u-margin--small { padding: 10px; }`;

export default test;